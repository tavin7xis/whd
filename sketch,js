function setup() {
  createCanvas(600, 600);
  background ("white");
}

function draw() {
 stroke ("black");
  strokeWeight (5);
  fill ("blue");
if (mouseIsPressed){
  rect(mouseX, mouseY, 40, 40);
 }
}
